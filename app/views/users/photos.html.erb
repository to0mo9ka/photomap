<% @user_photos.each do |place| %>
  
    <%= link_to image_tag(place.image_url, class: "square-image"), place.image_url, data: { lightbox: 'place', title: place.body } %>
       
    <!-- ここに写真の詳細リンクやその他の情報を追加 -->
  
<% end %>