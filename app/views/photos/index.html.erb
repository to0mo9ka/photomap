<div class="album">
  <% @places.each do |place| %>
    <span class="photo">
      <%= link_to image_tag(place.image_url, width: 100, height: 100), place.image_url, data: { lightbox: 'place' } %>
    </span>
  <% end %>
</div>
